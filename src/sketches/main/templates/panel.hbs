<div class="panel">
  <div class="panel__inner">
    <div class="spacer"></div>
    <div class="thumbnail">{{thumbnail}}</div>
    <div class="full-content">
      {{#each content}}
        <p>{{{.}}}</p>
      {{/each}}
    </div>
  </div>
</div>